import React from 'react';
import {View, Text} from 'react-native';

const FooterTab = () => {
    return (
        <View>
            <Text style={{alignContent: 'center', fontSize: 30, marginBottom: 20, color: '#fff', fontWeight: 'bold'}}> LAZADA </Text>
        </View>
    )
};

export default FooterTab;